{% assign products_to_show = section.settings.popularQuestionRange %}

<div class="popular-question-block" id="popularQuestionBlock" data-faqs-to-show="{{ products_to_show }}">
  <div class="popular-content-wrapper page-width">
    <div class="heading center">
      <h2>{{ section.settings.popularQuestionTitle }}</h2>
    </div>
    <div class="popular-card-wrapper" id="popularCards">
      <!-- Initially show skeleton loader -->
      <div class="skeleton-card" style="width: 504px; height: 468px;"></div>
      <!-- Add more skeleton loaders for each product/category item if needed -->
    </div>
  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  // Check if we're on a category page
  if (window.location.pathname.includes('product-category')) {
    fetchFAQsByFilters({ popular: true, appendTo: "popularCards" });
  }
});

document.addEventListener('shopify:section:load', (event) => { 
  const sectionId = event?.detail?.sectionId;
  if (sectionId.includes('evenflo_faq_popular_question')) {
    fetchFAQsByFilters({ popular: true, appendTo: "popularCards" });
  } 
});

function fetchFAQsByFilters(options) {
  const popularCardsContainer = document.getElementById(options.appendTo);
  
  // Simulate fetching category data
  setTimeout(() => {
    // Remove skeleton loader once the data is fetched
    popularCardsContainer.innerHTML = `<div>FAQ Content Loaded for Product Category</div>`; // Replace with actual fetched content
  }, 2000); // Simulated fetch delay (2 seconds)
}
</script>

<style>
/* Skeleton Loader Styles */
.skeleton-card {
  background-color: #e0e0e0;
  animation: skeleton 1.5s infinite ease-in-out;
  border-radius: 4px;
  margin-bottom: 10px;
}

/* Skeleton animation */
@keyframes skeleton {
  0% {
    background-color: #e0e0e0;
  }
  50% {
    background-color: #f0f0f0;
  }
  100% {
    background-color: #e0e0e0;
  }
}
</style>

{% schema %}
{
  "name": "EvenFlo FAQ Popular",
  "tag": "section",
  "class": "popularQuestion",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "popularQuestionTitle",
      "label": "Popular Question Title",
      "default":"Popular Questions"
    },
    {
        "type": "range",
        "id": "popularQuestionRange",
        "label": "Number of questions to show",
        "default":3,
        "min": 1,
        "max": 3,
        "step": 1
      }
  ],
  "presets": [
    {
      "name": "EvenFlo FAQ Popular",
      "blocks": [

      ]
    }
  ]
}
{% endschema %}
